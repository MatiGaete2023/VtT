# Dependencias del Transcriptor VtT (Windows, macOS y Linux).
# faster-whisper trae CTranslate2 + PyAV, por lo que NO necesitas instalar FFmpeg
# para transcribir. Requiere Python 3.9+.
faster-whisper>=1.2.1
# Word .docx estructurado.
python-docx>=1.1.2
# Diarizacion local opcional. Los modelos solo se descargan al activar hablantes.
sherpa-onnx>=1.13.7
# Descarga de audio de YouTube.
yt-dlp>=2024.8.6
# Grabacion desde microfono / entrada de audio.
sounddevice>=0.4.6
numpy>=1.24
# Captura del audio del sistema en Windows via WASAPI loopback.
soundcard>=0.4.2
