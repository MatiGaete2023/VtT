# Dependencias del Transcriptor Whisper (multiplataforma: Windows, macOS, Linux).
# faster-whisper trae CTranslate2 + PyAV, por lo que NO necesitas instalar FFmpeg
# para transcribir. yt-dlp permite bajar audio de YouTube.
faster-whisper>=1.0.0
yt-dlp>=2024.8.6
# Grabacion desde el microfono / entrada de audio (PortAudio incluido en las
# ruedas de Windows/macOS; en Linux instala ademas: sudo apt install libportaudio2).
sounddevice>=0.4.6
